/**
 * /src/ui/DiagnosticFlow.tsx
 * PRESENTATION ONLY
 */

// TODO: UI flow for selecting entry anchor, gathering observations, clarifiers, then showing results.

export function DiagnosticFlow() {
  return null;
}
